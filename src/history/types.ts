export type HistoryEntry = {
  id: string;
  seriesId: string;
  seriesName: string;
  folder: string;
  pdfFilename: string;
  coverFilename: string;
  volumeTitle: string;
  lastPage: number;
  readAt: number;
};
